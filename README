== BUGS
exit | exit => pipe avec cmds qui generent des erreurs (cmd caca | cmd caca)
expand dans heredoc seulement si heredoc_query n'est pas entre quote
ne pas expand le heredoc_query pour ne pas quit le heredoc si on ecrit la valeur de la variable
>>      expand heredoc fait mais expand + quote + heredoc [cat << $"USER"] to do

cat << 34 << 123 << 123 >txt3.txt | cat << 32 << 12 << 123 > txt1.txt | cat <Makefile > final.txt
Si ctrl D, tout est exit au lieu de juste un heredoc et toute les pipes leak;

Attention ne pas ouvrir plusieurs heredoc a la fois, tout traiter en avance ?


----RESOLU----

cat <Makefile <testfile.txt <Makefile <testfile <fuck.txt > lala.txt >> loulou.txt | wc -l > 123.txt | echo MIAOU   V
FD OUVERT

<<<<<<< HEAD

echo $? $oui $USER $_ $$ '$USER' '$_' ouiu $? '$?'

oui non oui non oui non oui non oui non oui non


cat Makefile | cat > out  => exit minishell after 2 exec
=======
(i > 0 && msg[i - 1] && msg[i + 1] && msg[i - 1] == '\"' && msg[i] == '$' && msg[i + 1] == '\"') || (msg[i] == '$' && !is_in_quotes(msg, i) && msg[i + 1] == '"')
>>>>>>> refs/remotes/origin/main
